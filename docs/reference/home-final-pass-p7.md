# Home final pass P7

Reference: https://labs.chaingpt.org/

This pass deliberately freezes scope to `/` only. Secondary routes remain dormant and are not linked from the homepage.

## Composition contract

1. Header / system stack placeholder navigation.
2. Hero: oversized two-line display typography + central drilling telemetry object + telemetry HUD.
3. Stack band: compact ecosystem-equivalent modules.
4. Beyond Measurement: single pinned laboratory state machine (orientation → formation → telemetry → surface).
5. Two loops / one system: paired full-height engineering programs.
6. Equipment: indexed service-like rows with one shared sticky BHA schematic.
7. Systems in the field: horizontally scrubbed portfolio-style rail.
8. Drill Monitor: surface software editorial block with scroll-drawn plan/fact path.
9. Field principles: large three-card proof band.
10. FAQ.
11. Final CTA/footer.

## Motion contract

- Lenis owns smooth scrolling.
- GSAP ScrollTrigger owns scrub/pin timelines.
- React state is not used on the hot scroll path.
- One timeline per narrative scene.
- Headings use clip/reveal plus restrained scramble/flicker.
- Persistent decorative rotations are avoided; movement is coupled to entry/scroll/pointer intent.
- Reduced-motion removes pin-heavy choreography.

## 3D boundary

All current drilling objects are CSS media placeholders. Do not refine them as final art. They define size, placement and motion ownership only. Replace later with project-owned alpha video / 3D assets without changing the page choreography.
